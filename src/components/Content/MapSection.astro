---
interface Props {
    title: string;
    images?: string[];
}

const { title, images = [] } = Astro.props;
---

<div class="map-section">
    <!-- The horizontal connector belonging to this section -->
    <div class="section-h-connector"></div>

    <div class="section-header">
        <h3 class="section-title">{title}</h3>
        {
            images.length > 0 && (
                <button
                    class="toggle-illustrations-btn"
                    aria-expanded="false"
                    title="Ver ilustraciones"
                >
                    <svg
                        xmlns="http://www.w3.org/2000/svg"
                        width="16"
                        height="16"
                        viewBox="0 0 24 24"
                        fill="none"
                        stroke="currentColor"
                        stroke-width="2"
                        stroke-linecap="round"
                        stroke-linejoin="round"
                    >
                        <>
                            <circle cx="12" cy="12" r="10" />
                            <line x1="12" y1="16" x2="12" y2="12" />
                            <line x1="12" y1="8" x2="12.01" y2="8" />
                        </>
                    </svg>
                    <span>Ilustraciones</span>
                </button>
            )
        }
    </div>

    {
        images.length > 0 && (
            <div class="illustrations-accordion" hidden>
                <div class="illustrations-grid">
                    {images.map((img) => (
                        <div class="illustration-item">
                            <img
                                src={img}
                                alt={`Ilustración de ${title}`}
                                loading="lazy"
                                class="presentational-image"
                            />
                        </div>
                    ))}
                </div>
            </div>
        )
    }

    <div class="section-content">
        <slot />
    </div>
</div>

<style>
    .map-section {
        position: relative;
    }

    .section-h-connector {
        position: absolute;
        left: -2rem;
        top: 0.7rem;
        width: 1.5rem;
        height: 2px;
        background: var(--color-map-line, #64748b);
    }

    .section-header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 1rem;
        margin-bottom: 0.6rem;
    }

    .section-title {
        margin: 0;
        color: var(--color-primary);
        font-weight: 400;
        font-size: 1.1rem;
        letter-spacing: -0.01em;
    }

    /* Estilos Botón "Ilustraciones" (Toggle) */
    .toggle-illustrations-btn {
        display: flex;
        align-items: center;
        gap: 6px;
        padding: 4px 10px;
        background: var(--bg-secondary);
        border: 1px solid var(--color-border);
        border-radius: 6px;
        color: var(--text-secondary);
        font-family: "Space Grotesk", sans-serif;
        font-size: 0.7rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.2s ease;
        flex-shrink: 0;
    }

    .toggle-illustrations-btn:hover {
        background: var(--bg-primary);
        color: var(--color-primary);
        border-color: var(--color-border);
    }

    .toggle-illustrations-btn[aria-expanded="true"] {
        background: rgba(37, 99, 235, 0.1);
        color: var(--color-primary);
        border-color: var(--color-primary);
    }

    /* Acordeón de Ilustraciones */
    .illustrations-accordion {
        margin-bottom: 1.5rem;
        margin-top: 1rem;
        padding: 0;
        background: transparent;
        border-radius: 0;
        border: none;
        animation: slideDown 0.3s ease-out;
    }

    .illustrations-accordion[hidden] {
        display: none;
    }

    .illustrations-grid {
        display: flex;
        flex-direction: column;
        gap: 1rem;
    }

    .illustration-item {
        width: 100%;
        border-radius: 12px;
        overflow: hidden;
        background: var(--bg-secondary);
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
        border: 1px solid var(--color-border);
    }

    .illustration-item img {
        display: block;
        width: 100%;
        height: auto;
        object-fit: contain;
        /* Imagen ocupa todo el ancho sin padding */
    }

    /* Modo oscuro para ilustraciones */
    :global(html.dark) .illustration-item {
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.3);
    }

    :global(html.dark) .illustration-item img {
        /* Fondo claro para imágenes con transparencia en modo oscuro */
        background: linear-gradient(135deg, #f5f7fa 0%, #e2e8f0 100%);
    }

    @keyframes slideDown {
        from {
            opacity: 0;
            transform: translateY(-10px);
        }
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    .section-content {
        display: flex;
        flex-direction: column;
        gap: 0.8rem;
    }
</style>
