---
interface Props {
  summary?: string;
  class?: string;
  open?: boolean;
}

const {
  summary = 'Respuesta',
  class: className = '',
  open = false,
} = Astro.props;
---

<details class={`respuesta ${className}`.trim()} open={open}>
  <summary>{summary}</summary>
  <slot />
</details>
