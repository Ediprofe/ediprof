---
/**
 * Página principal - Refactorizada
 * Usa componentes modulares para mejor mantenibilidad
 */
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Layout/Header.astro';
import Footer from '../components/Layout/Footer.astro';
import HeroSection from '../components/Sections/HeroSection.astro';
import SectionHeader from '../components/UI/SectionHeader.astro';
import MateriaCard from '../components/Content/MateriaCard.astro';
import { getAllLessons, getMateriasInfo } from '../utils/collections';

// Obtener datos
const allLessons = await getAllLessons();
const materias = await getMateriasInfo();

// Stats para el hero
const heroStats = [
  { value: materias.length, label: 'Materias' },
  { value: `${allLessons.length}+`, label: 'Lecciones' },
  { value: '∞', label: 'Recursos' }
];
---

<BaseLayout title="Inicio" showTopBar={false}>
  <div class="home-container">
    <Header />

    <HeroSection
      title="Guía Educativa para"
      highlightedText="Ciencias y Matemáticas"
      subtitle="Contenido estructurado con videos explicativos, ejercicios prácticos y recursos descargables. Aprende a tu ritmo con lecciones diseñadas para maximizar tu comprensión."
      showSocial={false}
      showStats={true}
      stats={heroStats}
      primaryCta={{ text: "Comenzar Ahora", href: "#materias" }}
    />

    <main class="main-content">
      {materias.length > 0 ? (
        <section class="materias-section" id="materias">
          <SectionHeader
            badge="Explora"
            title="Materias Disponibles"
            description="Elige tu área de estudio y comienza tu viaje de aprendizaje"
          />
          
          <div class="materias-grid">
            {materias.map((materia, index) => (
              <MateriaCard
                slug={materia.slug}
                name={materia.name}
                lessonCount={materia.lessonCount}
                delay={index * 0.1}
              />
            ))}
          </div>
        </section>
      ) : (
        <section class="coming-soon-global">
          <div class="coming-soon-container">
            <div class="coming-soon-badge">🚀 Próximamente</div>
            <h2 class="coming-soon-heading">Estamos preparando contenido de calidad</h2>
            <p class="coming-soon-text">Lecciones de ciencias y matemáticas con explicaciones claras, ejercicios prácticos y recursos descargables..</p>
          </div>
        </section>
      )}
    </main>

    <Footer variant="full" />
  </div>
</BaseLayout>

<style>
  .home-container {
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    background: var(--bg-primary);
  }

  .main-content {
    flex: 1;
    max-width: 1200px;
    margin: 0 auto;
    padding: 4rem 1.5rem;
    width: 100%;
  }

  .materias-section {
    margin-bottom: 4rem;
  }

  .materias-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 1.5rem;
  }

  @media (max-width: 768px) {
    .materias-grid {
      grid-template-columns: 1fr;
    }
  }

  /* Coming Soon Global */
  .coming-soon-global {
    display: flex;
    justify-content: center;
    align-items: center;
    min-height: 50vh;
    padding: 3rem 1.5rem;
  }

  .coming-soon-container {
    text-align: center;
    max-width: 500px;
  }

  .coming-soon-badge {
    display: inline-block;
    background: linear-gradient(135deg, #fbbf24, #f59e0b);
    color: #1a1a1a;
    font-size: 0.8125rem;
    font-weight: 700;
    padding: 0.5rem 1rem;
    border-radius: 100px;
    margin-bottom: 1.5rem;
  }

  .coming-soon-heading {
    font-size: 1.75rem;
    font-weight: 700;
    color: var(--text-primary);
    margin: 0 0 1rem;
    line-height: 1.3;
  }

  .coming-soon-text {
    color: var(--text-secondary);
    font-size: 1rem;
    line-height: 1.6;
    margin: 0 0 1.5rem;
  }

  .coming-soon-subjects {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 0.5rem;
  }

  .subject-tag {
    background: var(--bg-secondary);
    border: 1px solid var(--border-color);
    padding: 0.5rem 1rem;
    border-radius: 100px;
    font-size: 0.875rem;
    color: var(--text-secondary);
  }
</style>
